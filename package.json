{
  "name": "lumber-and-hunt",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build --base=./",
    "preview": "vite preview",
    "test": "node --test tests/*.test.mjs",
    "sim": "node sim/simulate.mjs"
  },
  "devDependencies": {
    "@types/three": "^0.186.0",
    "typescript": "~6.0.2",
    "vite": "^8.3.0"
  },
  "dependencies": {
    "three": "^0.186.0"
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
